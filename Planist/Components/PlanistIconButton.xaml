<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Planist.Components.PlanistIconButton"
             x:Name="iconButton">
    <Border x:Name="iconContainer" Background="{StaticResource Secondary}">

        <Border.StrokeShape>
            <RoundRectangle CornerRadius="10" />
        </Border.StrokeShape>
        
        <ImageButton x:Name="icon" Command="{Binding Command, Source={x:Reference iconButton}}">
        </ImageButton>
        
    </Border>
</ContentView>
